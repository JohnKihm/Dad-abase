<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dadabase - Where Puns and Parenting Come Together</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/output.css">
  <link rel="stylesheet" href="/css/style.css">
  <script src="https://unpkg.com/tone"></script>
</head>

<body>
  <div>
    <header>
      <h1>
        Dad Joke Generator 
      </h1>
      <nav>
        <a href="/">Home</a>
        <a href="/joke">Jokes</a>
        <a href="/profile">Profile</a>
        {{#if logged_in}}
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </header>
     <main class="container mx-auto py-8 flex-grow grid grid-cols-1 md:grid-cols-2 gap-8">
      {{{ body }}}
    </main>
    <footer class="text-center text-sm py-1 mt-auto fixed bottom-0 w-full">
        <div class="container mx-auto">
            <p>&copy; 2024 OnlyDadsLLC</p>
        </div>
    </footer>
  </div>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
